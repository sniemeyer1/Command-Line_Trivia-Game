const hello = require('../app.js');
const assert = require('assert');

describe("getQuestion", function(){
it("should return an index of question", function(){
    assert.strictEqual(getQuestion(quizData, numberOfQuestions), Number);
 });
});

